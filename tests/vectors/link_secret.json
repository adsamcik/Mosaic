{
  "schemaVersion": 1,
  "operation": "link.generate-secret.v1",
  "protocolVersion": "mosaic-v1",
  "rust_canonical": false,
  "description": "generate_link_secret returns 32 fresh CSPRNG bytes; only the length is byte-checked.",
  "algorithms": {
    "rng": "OS-CSPRNG via getrandom (Rust) / sodium.randombytes_buf (TS)"
  },
  "domainLabels": [
    "mosaic.v1.link.secret"
  ],
  "inputs": {},
  "expected": {
    "lengthBytes": 32
  },
  "negativeCases": [
    {
      "name": "no-rng",
      "mutations": [
        "simulate getrandom failure"
      ],
      "errorCode": "RNG_FAILURE"
    }
  ],
  "serverBoundOutputs": [],
  "forbiddenServerOutputs": [
    {
      "name": "linkSecret",
      "reason": "Generated secret is delivered in the URL fragment and must never be uploaded.",
      "errorCode": "PLAINTEXT_KEY_FORBIDDEN"
    }
  ]
}
