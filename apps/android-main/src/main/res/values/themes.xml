<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Minimal AppCompat theme. AGP requires the Application's android:theme to resolve
       to a real style; AppCompat is the smallest well-formed choice that does not
       drag in Material/Compose. -->
  <style name="Theme.MosaicMain" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:windowBackground">@android:color/black</item>
    <item name="android:textColorPrimary">@android:color/white</item>
    <item name="android:textColorSecondary">@android:color/darker_gray</item>
  </style>
</resources>
